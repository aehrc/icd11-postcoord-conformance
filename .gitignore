# Generated by run.sh — large, transient, per-run artifacts.
onto-results.json
icdapi-results.json
onto-cases.json
icdapi-cases.json
comparison.json

# Jekyll build artifacts (docs/ is the Pages source; GitHub builds it remotely).
_site/
.jekyll-cache/
.jekyll-metadata
